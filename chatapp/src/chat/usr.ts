
class ChatUsr {
	/* What does the user need for the chat? Name, A for Admin, O for Owner, Image for Avatar, if user is on/offline */
	name: string;
	admin: string;
	owner: string;
	avatar: image;
	status: boolean;
}